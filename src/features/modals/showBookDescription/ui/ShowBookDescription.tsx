import React from 'react';
import Block from "../../../../shared/ui/Block/Block.tsx";

const ShowBookDescription:React.FC = () => {
    return (
        <Block bgColor="var(--black-color)">
            Description
        </Block>
    );
};

export default ShowBookDescription;